import React from "react";
import { connect } from "react-redux";
import "./Timer.css";

const Timer = props => <p className="timer">0:00</p>;

const mapStateToProps = state => ({});

export default Timer;
